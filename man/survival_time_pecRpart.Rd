% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_tree-rpart.R
\name{survival_time_pecRpart}
\alias{survival_time_pecRpart}
\title{A wrapper for survival time predictions with pecRpart models}
\usage{
survival_time_pecRpart(object, new_data)
}
\arguments{
\item{object}{A parsnip \code{model_fit} object resulting from \link[parsnip:details_decision_tree_rpart]{decision_tree() with engine = "rpart"}.}

\item{new_data}{Data for prediction.}
}
\value{
A numeric vector of predicted survival times.
}
\description{
Returns the median survival time of each leaf's Kaplan-Meier curve, as
stored in \code{pecRpart}'s stratified \code{prodlim} fit. \code{Inf} is returned when the
leaf's KM never crosses 0.5.
}
\examples{
\dontshow{if (rlang::is_installed("pec")) withAutoprint(\{ # examplesIf}
mod <- decision_tree() |>
  set_mode("censored regression") |>
    set_engine("rpart") |>
    fit(Surv(time, status) ~ ., data = lung)
survival_time_pecRpart(mod, new_data = lung[1:3, ])
\dontshow{\}) # examplesIf}
}
\keyword{internal}
